<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manya.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Тест &quot;Умники и умницы&quot;"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SubjectsActivity" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".RusActivity" />
        <activity android:name=".MathActivity" />
        <activity android:name=".NatureActivity" />
        <activity android:name=".Quest1RusActivity"></activity>
    </application>

</manifest>